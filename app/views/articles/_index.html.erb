<% @article.each do |article|%>

  <h2><%= article.title %></h2>
   
  <div>
    <%= article.content %>
  </div>

   <div>
   <%= image_tag article.image.url(:thumb)%>
   </div>

  <div>
    <%= link_to 'Chi tiết', article_path(article)%>
    <% if user_signed_in? %>
      <% if current_user == article.user %>
        <div class="article-info">
          <%= link_to 'Edit', edit_article_path(article)%>
          <%= link_to 'delete', article_path(article), method: 'delete', data: {confirm: 'Bạn muốn xóa?'} %>
        </div> 
      <%end%> 
    <%end%>
  </div>

<%end%>
