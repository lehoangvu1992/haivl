<% @article.each do |article|%>

   <h2><%= article.title %></h2>
   
   <div>
   <%= article.content %>
   </div>

   <div>
    <%= link_to 'chi tiet', article_path(article)%>
    <%if user_signed_in?%>
    <%= link_to 'Edit', edit_article_path(article)%>
    <%= link_to 'delete', article_path(article), method: 'delete', data: {confirm: 'ban co muon xoa?'} %>
    <%end%>
   </div>



<%end%>
